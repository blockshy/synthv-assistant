# 贡献与问题反馈

欢迎提交问题和改进。本项目目前以 Windows、Synthesizer V Studio 2 Pro 2.2.1 独立版为基准，真实云模型尚未完成验证。请明确区分模拟测试、真实宿主测试和真实供应商调用，避免把其中一种结果扩大为其他场景的保证。

## 准备开发环境

从源码仓库运行，按照 [README](README.md#首次准备) 创建项目 `.venv`。测试另外安装可选依赖：

```powershell
# 在项目根目录执行；依赖仅安装到本项目虚拟环境。
.\.venv\Scripts\python.exe -m pip install -e '.[test]'

# 默认测试使用临时文件、模拟宿主与虚构凭据，不需要正在打开的工程或真实模型服务。
.\.venv\Scripts\python.exe -m unittest discover -s tests -v

# 修改采集器后，需要 .NET SDK 10，在本机重新构建。
.\.venv\Scripts\python.exe -m synthv_assistant build-capture

# 修改前端后检查共享规范；本检查使用 Node.js 做语法检查，工作台运行本身不依赖 Node.js。
.\.venv\Scripts\python.exe scripts/check_frontend.py
```

自动化测试应独立创建临时数据目录，不读取默认 `data/`、真实环境密钥或当前打开的工程。新增模型协议测试使用模拟 HTTP 响应；新增音频测试使用合成样本。需要真实 SynthV 验证时，使用自行创建的最小测试工程并另存工作副本，清楚记录版本、声库、输出方式和操作范围，不把工程及录音提交到仓库。

## 修改要求

- 为新增代码提供规范、具体的中文注释，重点解释状态约束、锁的作用、失败处理和安全边界。
- 保持默认只读、模型建议先预览再确认、未知结果禁止自动重放；不要添加任意 Lua 执行、任意文件路径或隐式云端调用。
- 保留大小、时间、数量限制及错误脱敏；涉及文件更新时遵守现有锁顺序、版本检查和原子替换约定。
- 保持纯文字请求不依赖音频模型、历史音频不自动重发、平台配置只影响后续请求。
- 针对实际行为变化补充有意义的回归测试。界面改动遵循 [前端设计与实现规范](docs/frontend-guidelines.md)，检查系统/浅色/深色主题、键盘操作、390/1150/1440 px 宽度与长内容，不依赖私人素材验收。
- 修改行为或限制时同步文档。完整工作台依赖源码树中的 `web/`、`synthv/` 和 `native/`，请勿把仅 Python 包安装描述为完整部署。

前端继续使用轻量原生 JavaScript/CSS，不新增框架或构建链。SVG 图标与管理按钮统一经 `web/ui.js` 创建和更新；尺寸、主题令牌、可访问名称及落地检查表见 [前端规范](docs/frontend-guidelines.md#规范落地检查表)。改造外观时必须保留忙碌、错误、模型真实摘要和工程确认状态。

## Issue 与 Pull Request

一般问题请描述版本、复现步骤、预期行为与实际结果；仅附必要且已脱敏的错误信息。界面问题可给出窗口尺寸及脱敏截图；录音问题说明系统 build、输出方式和是否为非静音，不上传私人演唱或商业伴奏。

Pull Request 请说明解决的问题、行为变化、验证方式与尚未覆盖的范围。提交前检查暂存内容，确保没有 `.env`、运行配置、工程、录音、截图、构建产物或本机验收报告。不要为了修复文档链接而将 `data/` 加入版本控制。敏感问题请按 [安全说明](SECURITY.md#报告安全问题) 处理。

项目使用 [MIT 许可证](LICENSE)。引入第三方文件时保留其许可与版权声明，并更新 [第三方声明](THIRD_PARTY_NOTICES.md)；不要提交 SynthV 软件、声库或其他无权分发的内容。
