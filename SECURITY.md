# 安全与隐私说明

本项目是面向可信 Windows 本机账户的实验性助手，不是多人服务或互联网网关。HTTP 仅监听回环地址；不要通过端口转发、公共反向代理或关闭来源检查将它对外开放。项目尚未经过独立安全审计。

## 数据与凭据

- 模型设置使用当前 Windows 账户的 DPAPI 加密。它保护磁盘上的配置，不防御已能以同一账户运行的恶意进程；迁移账户或机器时可能需要重新配置。
- 密钥不应写入源码、浏览器存储、URL、截图或问题报告。保存配置不会验证密钥或调用模型；更换供应商或 API 根地址需重新填写密钥。
- 会话、歌词摘要、录音、上传素材、工程备份、IPC 和操作日志属于私人数据，未统一加密。默认位于 `data/`；自定义数据目录具有相同敏感性。
- 文本发送包含当前消息与受限文字历史；只有明确附带的选区摘要和本次音频进入相应模型请求。上传素材到本地、录音、保存设置本身不调用云模型。显式刷新模型目录会向选定供应商发送认证请求。
- 自定义 HTTPS 端点会接收对应凭据及请求内容。供应商如何保留或处理已发送内容，应以其政策和账户设置为准；删除本地文件不会删除供应商持有的副本。
- MCP 客户端可以请求工程信息和录音。请检查客户端的工具权限、模型数据政策及人工确认设置，不能将其视为仅能读取脱敏聊天摘要的入口。

## 工程与删除边界

工程默认为只读。开启写入前，后端备份磁盘上已经保存的 `.svp`；未保存编辑不在该备份内。正式应用依赖有效预览及状态校验，超时或结果未知时不要重复写入。MCP 的逐次人工审批由客户端负责。

移入回收站保留原始资料。永久删除要求明确确认，仅清理选定资源，不撤销工程修改、不级联其他会话、备份或日志，也不是安全擦除。删除中断的资源会保持不可用，并允许重试永久删除。会话中可能继续保留附件名称等历史文字。

## 报告安全问题

请先查看仓库是否开放 GitHub 的私密漏洞报告入口（Security → Report a vulnerability）；可用时优先使用它。如果该入口不可用，可先在 Issue 中仅说明需要私下联系的安全问题类型，不要公开密钥、利用细节、私人工程或可识别的录音，等待维护者提供私密渠道。当前不承诺固定响应时间。

报告请提供受影响版本、系统与 SynthV 版本、问题范围、最小复现步骤和预期影响，使用虚构凭据与自制最小工程。若真实密钥已经泄露，应先在供应商处撤销或轮换，再清理公开内容及相关提交历史。
